<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cif.dao.Curve3DMapper">
    
    <select id="getAllCurve3Ds" resultType="Curve3DBean">
        select * from curve3ds where categoryID = #{categoryID}
    </select>
    
    
    <select id="getCurve3D" resultType="Curve3DBean">
        select * from curve3ds where id = #{id}
    </select>

    <insert id="addCurve3D" parameterType="Curve3DBean" useGeneratedKeys="true" keyProperty="id">
        insert into curve3ds(name,categoryID,owner,
        startDepth, endDepth, depthLevel, dataType,
        curveUnit, depthUnit, propertiesEX, file,
        timeSampleNum,timeSampleStartTime,timeSampleLevel,timeSampleUnit,
        arrayNum, createdAt) 
          values( #{name}, #{categoryID}, #{owner},
          #{startDepth}, #{endDepth}, #{depthLevel}, #{dataType},
          #{curveUnit}, #{depthUnit}, #{propertiesEX}, #{file},
          #{timeSampleNum},#{timeSampleStartTime},#{timeSampleLevel},#{timeSampleUnit},
          #{arrayNum},now())
    </insert>
    
    <update id="updateCurve3D">
        update curve3ds set name = #{name}, 
        categoryID = #{categoryID},
        owner = #{owner}, 
        startDepth= #{startDepth}, 
        endDepth= #{endDepth}, 
        depthLevel= #{depthLevel}, 
        dataType= #{dataType},
        curveUnit= #{curveUnit}, 
        depthUnit= #{depthUnit}, 
        propertiesEX= #{propertiesEX}, 
        file= #{file},
        timeSampleNum = #{timeSampleNum},
        timeSampleStartTime = #{timeSampleStartTime},
        timeSampleLevel = #{timeSampleLevel},
        timeSampleUnit = #{timeSampleUnit},
        arrayNum = #{arrayNum},
        updatedAt = now()
          where id = #{id}
    </update>
    
    <delete id="deleteCurve3D"> 
        delete from curve3ds where id = #{id} 
    </delete>
    
</mapper>